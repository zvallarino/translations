# Translation pipeline requirements.
#
# IMPORTANT: install PyTorch SEPARATELY first, since the correct wheel depends
# on your CUDA driver version. Do NOT install it from this file.
#
#   # Check your CUDA version:
#   nvidia-smi
#
#   # Then install the matching torch wheel. For CUDA 12.1:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
#
#   # For CUDA 11.8:
#   pip install torch --index-url https://download.pytorch.org/whl/cu118
#
#   # CPU only (very slow — not recommended for NLLB):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#
# After torch is installed, run:
#   pip install -r requirements.txt
 
# Hugging Face stack for NLLB-200
transformers>=4.40.0
sentencepiece>=0.2.0        # required by NLLB tokenizer
accelerate>=0.30.0          # makes model loading faster + avoids warnings
safetensors>=0.4.0          # transformers uses this for model weights
 
# Language ID
# Use fasttext-wheel (prebuilt binary wheels) instead of fasttext (source-only,
# fails to build on Python 3.12+). Same import name and API.
fasttext-wheel>=0.9.2
 
# Spreadsheet I/O
pandas>=2.0.0
openpyxl>=3.1.0